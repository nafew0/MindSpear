# MindSpear

This deployment guide is superseded.

For production hosting, follow [READMEv2.md](READMEv2.md). It is the final server1 reset and clean deploy guide, and it uses same-domain routing on `https://mindspear.app`.

Do not use the old separate API/WebSocket host deployment path.
